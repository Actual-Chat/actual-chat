# Default nginx worktree port mappings (main project only)
# For multi-worktree support, c.ps1 sets NGINX_WORKTREE_PORTS_CONF to artifacts/nginx-worktree-ports.conf

map $worktree_name $backend_port {
    default 7080;
}
