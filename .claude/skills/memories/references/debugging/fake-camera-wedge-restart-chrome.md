- After a video session run under CDP "Slow 3G" throttling (`mcp__chrome1__emulate`), `getUserMedia({video:true})` started failing with `NotReadableError: Could not start video source` in ALL three automation Chromes (9222/9223/9224), even in a fresh tab. Windows camera consent was Allow and nothing held the camera. A reload or new tab does not clear it.
- Fix: restart that Chrome with its exact original command line: `Get-CimInstance Win32_Process -Filter "Name='chrome.exe'" | ? { $_.CommandLine -match 'remote-debugging-port' -and $_.CommandLine -notmatch '--type=' }` gives PID + args; `Stop-Process`, then `Start-Process chrome.exe -ArgumentList <args>`; poll `http://127.0.0.1:<port>/json/version`. The chrome1 MCP bridge reconnects by itself. (2026-09-02: restarted 9222 only; 9223 and 9224 were still wedged.)
- Under Slow 3G the JoinVideoCallModal warmup fails ("Camera is unavailable", Start button disabled) — start the camera unthrottled, then apply the throttle for the step you want slow.
- Desktop shows the audio-panel VideoToggle (`.video-wrapper button`) only while the mic is recording (`.rec-btn` first); the join modal's confirm is the `Start video` button; the own tile is `.video-streaming-preview` with `data-starting` until the first frame.
- Observed but unexplained: under throttle the DOM click → Blazor WASM handler latency varied 40 ms–8 s with an idle JS loop (probe: wrap `ui.TuneUI.playAndWait`, which every Button click hits via `this`).

**How to apply:** if a rig run says the camera is unavailable, restart that Chrome instead of debugging the app. See [[chrome-mcp-and-cdp-rig]].
