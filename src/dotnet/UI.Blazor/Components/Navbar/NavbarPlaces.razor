@using ActualChat.UI.Blazor.Services
@namespace ActualChat.UI.Blazor.Components
@inject FeedbackUI FeedbackUI

<div class="flex-x items-center">
    <Tooltip Text="Imaginary Place" Position="TooltipPosition.Right">
        <ButtonSquare
            Click="@OnClick"
            Class="first-pinned">
            IP
        </ButtonSquare>
    </Tooltip>
    <div class="ml-4 md:hidden">Imaginary Place</div>
</div>

<div class="flex-x items-center">
    <Tooltip Text="Create Place" Position="TooltipPosition.Right">
        <ButtonSquare
            Class="plus-chat"
            Click="@OnClick">
            <i class="icon-plus text-2xl font-medium"></i>
        </ButtonSquare>
    </Tooltip>
    <div class="ml-4 md:hidden">Create Place</div>
</div>

@code {
    private Task OnClick()
        => FeedbackUI.AskFeatureRequestFeedback("places", "Places");
}
