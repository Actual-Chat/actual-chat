@using ActualChat.UI.Blazor.Components.Dropdown
@namespace ActualChat.UI.Blazor.Components

@{
    var leftContainerClass = "flex-auto float-left mr-4 ml-1 h-full";
    var rightContainerClass  = "flex-auto float-right mx-1 h-full";
}

<div class="relative">
    @if (IsHorizontal()) {
        <button @onclick="OnItemClick" tooltip="@Title" flow="@TooltipFlow()" class="dropdown-menu-item flex h-8 w-full text-primary @GetCss()" name="@Name">
            <div class="icon-container relative flex-auto mx-1 h-full">
                <div class="text-primary rounded-sm text-xxs relative">@Icon</div>
            </div>
        </button>
    } else {
        <button @onclick="OnItemClick" class="dropdown-menu-item flex h-8 w-full text-primary @GetCss()" name="@Name">
            @if (IsIconOnLeft()) {
                <div class="icon-container w-8 @leftContainerClass">
                    <div class="flex">@Icon</div>
                </div>
                <div class="title-container w-full @rightContainerClass">
                    <div class="flex">@Title</div>
                </div>
            } else {
                <div class="title-container w-full @rightContainerClass">
                    <div class="flex">@Title</div>
                </div>
                <div class="icon-container w-10 @leftContainerClass">
                    <div class="flex">@Icon</div>
                </div>
            }
        </button>
    }
</div>

@code {
    [Parameter]
    public EventCallback OnItemClick { get; set; }
    [Parameter]
    public string Title { get; set; } = "";
    [Parameter]
    public RenderFragment? Icon { get; set; }
    [Parameter]
    public string Class { get; set; } = "";
    [Parameter]
    public string Name { get; set; } = "";
    [CascadingParameter]
    public DropdownMenu? Menu { get; set; }

    private string GetCss()
        => $"{Class} {Menu?.ItemClass}";

    private bool IsIconOnLeft()
        => Menu?.IconPosition == IconPosition.Left;

    private string TooltipFlow()
        => Menu?.TooltipPosition.ToString()?.ToLower() ?? "top";

    private bool IsHorizontal()
        => Menu?.Orientation == Orientation.Horizontal;
}
