@using ActualChat.UI.Blazor.Services
@namespace ActualChat.UI.Blazor.Components
@{
    var mauiAppBuildTime = (int)LoadingUI.MauiAppBuildTime.TotalMilliseconds;
    var appInitTime = (int)LoadingUI.AppInitTime.TotalMilliseconds;
    var appAboutRenderContentTime = (int)LoadingUI.AppAboutRenderContentTime.TotalMilliseconds;
    var appStartTime = (int)LoadingUI.LoadingTime.TotalMilliseconds;
}

@if (appStartTime > 0) {
    <div class="blazor-mode-toggle px-1">
        @if (mauiAppBuildTime > 0) {
            @(mauiAppBuildTime + "/")
        }
        @($"{appInitTime}/{appAboutRenderContentTime}/{appStartTime}ms")
    </div>
}

@code {
    [Inject] LoadingUI LoadingUI { get; init; } = null!;
}
