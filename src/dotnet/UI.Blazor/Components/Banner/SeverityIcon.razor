@namespace ActualChat.UI.Blazor.Components
@inherits FusionComponentBase
@{
    var color = $"banner-{Severity.ToString().ToLowerInvariant()}";
    var circleBgColorCls = $"bg-{color}";
    var iconColorCls = $"text-{color}-icon";
}
<div class="severity-icon @circleBgColorCls">
     <i class="@Icon @iconColorCls text-2xl"></i>
</div>

@code {
    [Parameter, EditorRequired] public BannerSeverity Severity { get; set; }

    private string Icon {
        get {
            var iconName = Severity switch
            {
                BannerSeverity.Success => "checkmark-circle",
                BannerSeverity.Info => "info",
                BannerSeverity.Error => "info",
                BannerSeverity.Warning => "alert-triangle",
                _ => "",
                };

            return $"icon-{iconName}";
        }
    }
}
