@namespace ActualChat.UI.Blazor.Components
@* Renders where the content considers itself on screen, and displays the layer it sits in. Costs
   nothing when that area isn't holding - see content-swap.ts. *@

<div class="content-swap-display" data-render-script-content-swap-display></div>
