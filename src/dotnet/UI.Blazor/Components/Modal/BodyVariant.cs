namespace ActualChat.UI.Blazor.Components;

public enum BodyVariant
{
    Default = 0, // 1rem inset
    Flush, // p-0, edge-to-edge (lists, contact selector, map, image)
    Tiles, // settings-style tiled rows
}
