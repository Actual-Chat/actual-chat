@using ActualChat.Users
@namespace ActualChat.UI.Blazor.Components
@inherits FusionComponentBase

@{
    RenderFragment RenderCircle(string color)
        => @<div class="absolute flex items-center justify-center right-0 bottom-0 w-2.5 h-2.5 rounded-full bg-01">
               <div class="@color rounded-full w-1.75 h-1.75"></div>
           </div>;
}

@switch (Presence) {
    case Presence.Online:
        @RenderCircle("bg-online");
        break;
    case Presence.Away:
        @RenderCircle("bg-away");
        break;
    case Presence.Recording:
        @RenderCircle("bg-icons-04 animate-pulse");
        break;
}

@code {
    [Parameter] public Presence Presence { get; set; }
}
