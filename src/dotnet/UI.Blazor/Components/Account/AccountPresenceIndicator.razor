@namespace ActualChat.UI.Blazor.Components
@using ActualChat.Users
@inherits ComputedRenderStateComponent<UIHub, Presence>
@{
    var presence = State.Value;
}

@PresenceFragments.PresenceIndicator(presence)

@code {
    private IUserPresences UserPresences => Hub.UserPresences;

    [Parameter, EditorRequired] public UserId? UserId { get; set; }

    protected override void OnInitialized()
        // This renders nothing but the presence itself, and presence recomputes far more often
        // than it changes - comparing the value is what keeps those recomputes off the screen
        => StateEqualityComparer = EqualityComparer<Presence>.Default;

    protected override ComputedState<Presence>.Options GetStateOptions() {
        if (UserId is null)
            return ComputedStateComponent.GetStateOptions(GetType(),
                static t => new ComputedState<Presence>.Options() {
                    InitialValue = Presence.Unknown,
                    Category = GetStateCategory(t),
                });

        var presenceComputed = Computed.GetExisting(() => UserPresences.Get(UserId, default));
        var presence = presenceComputed?.IsConsistent() == true &&  presenceComputed.HasValue
            ? presenceComputed.Value
            : Presence.Unknown;
        return new() {
            InitialValue = presence,
            Category = GetStateCategory(GetType()),
        };
    }

    protected override Task<Presence> ComputeState(CancellationToken cancellationToken)
        => UserId is null
            ? Task.FromResult(Presence.Unknown)
            : UserPresences.Get(UserId, cancellationToken);
}
