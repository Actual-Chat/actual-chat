<div class="page-with-header-and-footer h-full w-full flex-y flex-nowrap justify-between overflow-hidden @Class">
    <div class="flex-none flex-y w-full overflow-hidden shadow-md">
        @Header
    </div>
    <div class="flex-1 flex-x h-full overflow-hidden">
        <div class="flex-1 -flex-y h-full overflow-hidden">
            <div class="flex-none flex-y w-full overflow-hidden">
                @Footer
            </div>
            <div class="flex-1 flex-y overflow-y-auto @BodyClass">
                @Body
            </div>
        </div>
        @RightPanel
    </div>
</div>

@code {
    [Parameter] public string Class { get; set; } = "";
    [Parameter] public string BodyClass { get; set; } = "";
    [Parameter] public RenderFragment? Header { get; set; }
    [Parameter] public RenderFragment? Footer { get; set; }
    [Parameter] public RenderFragment? Body { get; set; }
    [Parameter] public RenderFragment? RightPanel { get; set; }
}
