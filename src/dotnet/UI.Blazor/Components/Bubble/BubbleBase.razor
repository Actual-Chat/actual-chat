@namespace ActualChat.UI.Blazor.Components
@implements IBubble

@code {
    [Parameter] public string Id { get; set; } = "";
    [Parameter] public bool IsLastVisible { get; set; }

    [CascadingParameter] public BubbleHost Host { get; set; } = null!;

    protected override async Task OnAfterRenderAsync(bool firstRender)
        => await Host.ShowBubble(this);
}
