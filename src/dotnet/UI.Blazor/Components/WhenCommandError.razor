@namespace ActualChat.UI.Blazor.Components
@if (Error != null) {
    <Error @key="@Error" Title="Action failed!" Dismissed="@Dismissed">@Error.Message</Error>
}

@code {
    [Parameter]
    public Exception? Error { get; set; }
    [Parameter]
    public EventCallback Dismissed { get; set; }
}
