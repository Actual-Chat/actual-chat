@using Stl.Extensibility
@implements IModalView<ImagePreviewModal.Model>
@attribute [MatchFor(typeof(Model), typeof(IModalView))]

<div>
    <img style="max-width: 800px; max-height: 600px" src="@ModalModel.Url" alt="@(ModalModel.AltText.NullIfEmpty() ?? "image")" />
</div>
<div>
    <a class="font-semibold text-sm cursor-pointer text-primary hover:underline hover:text-primary-inverted"
       href="@ModalModel.Url" target="_blank">Show original</a>
</div>

@code {
    [Parameter] public Model ModalModel { get; set; } = null!;

    public sealed record Model(string Url, string? AltText);
}
