@page "/unavailable"
@page "/unavailable/{What}"
@inherits ComponentBase<UIHub>

<MainHeader>@L.Unavailable_Title</MainHeader>

@(What.NullIfEmpty() is { } what ? L.Unavailable_NotFound_Format(what) : L.Unavailable_PageNotFound)

@code {
    [Parameter] public string? What { get; set; }
}
