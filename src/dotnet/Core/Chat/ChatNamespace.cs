namespace ActualChat.Chat;

// This type exists solely to ensure the ActualChat.Chat namespace is available
// in projects that don't reference Api (e.g., Core), so the global using doesn't fail.
internal static class ChatNamespace;
