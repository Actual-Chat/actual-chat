@namespace ActualChat.Chat.UI.Blazor.Components
@inherits ComputedStateComponent<UnreadMessagesSubHeader.Model>
@{
    var m = State.ValueOrDefault ?? Model.None;
    if (m.Count == 0)
        return;
}

<div class="unread-messages-sub-header" @onclick="Click">
    @GetUnreadCountMessage()
</div>

@code {
    [Inject] private Session Session { get; init; } = null!;
    [Inject] private UnreadMessagesFactory UnreadMessagesFactory { get; init; } = null!;

    private UnreadMessages UnreadMessages { get; set; } = null!;

    [CascadingParameter] public Chat Chat { get; set; } = null!;
    [Parameter] public EventCallback Click { get; set; }

    protected override Task OnParametersSetAsync() {
        UnreadMessages.DisposeSilently();
        UnreadMessages = UnreadMessagesFactory.Get(Chat.Id);
        return base.OnParametersSetAsync();
    }

    public override async ValueTask DisposeAsync() {
        await base.DisposeAsync();
        UnreadMessages.DisposeSilently();
    }

    protected override ComputedState<Model>.Options GetStateOptions() {
        return new() {
            InitialValue = Model.None,
            UpdateDelayer = new UpdateDelayer(UIActionTracker.None, 2),
        };
    }

    protected override async Task<Model> ComputeState(CancellationToken cancellationToken) {
        var count = await UnreadMessages.GetCount(cancellationToken);
        if (!count.HasValue)
            return Model.None;

        return new() {
            Count = count.Value,
        };
    }

    private string PluralizeMessage(int count) {
        return count switch {
            1 => "message",
            _ => "messages",
        };
    }

    private string GetUnreadCountMessage() {
        var model = State.ValueOrDefault;
        return model?.Count switch {
            > 0 and < 10 => $"{ model.Count } unread { PluralizeMessage(model.Count) }",
            > 0 => $"{ model.Count }+ unread messages",
            _ => string.Empty
        };
    }

    public sealed record Model {
        public static readonly Model None = new();

        public int Count { get; init; }
    }
}
