@using Stl.Extensibility
@inherits MarkupViewBase<ChatEntryKindMarker>
@attribute [MatchFor(typeof(ChatEntryKindMarker), typeof(IMarkupView))]
@{
    var cls = Markup.Kind == ChatEntryKind.Audio ? "icon-volume-up" : "icon-text";
}
<span class="chat-entry-kind-marker @cls"></span>
