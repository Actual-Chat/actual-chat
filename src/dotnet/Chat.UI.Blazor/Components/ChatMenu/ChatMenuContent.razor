@namespace ActualChat.Chat.UI.Blazor.Components
@inherits FusionComponentBase
@{
    var rules = ChatState.Chat.Rules;
}

@if (rules.CanWrite()) {
    <ChatMenuRecord/>
}
<ChatMenuPin/>
<ChatMenuListen/>
@if (rules.CanLeave()) {
    <ChatMenuLeave/>
}

@code{
    [CascadingParameter] public ChatState ChatState { get; set; } = null!;
}
