@namespace ActualChat.UI.Blazor.App.Components
@using ActualChat.UI.Blazor.App.Module
@inherits ComputedStateComponent<AppUIHub, ChatActivityPanel.Model?>
@{
    var m = State.Value;
    if (m is null)
        return;

    var isVisible = m.IsAnyoneTalking || m.IsAnyoneVideoStreaming || m.IsListening || m.IsRecordingHere;
    if (!isVisible)
        return;

    var chat = Chat;
    var iconSize = ScreenSize.IsNarrow() ? 6 : 8;
    var isListening = m.IsListening;
    var isRecordingHere = m.IsRecordingHere;
    var isVideoRecordingHere = m.IsOwnVideoRecording;
    var isParticipating = isListening || isRecordingHere || isVideoRecordingHere;
    var isActive = m.IsAnyoneTalking;
    var showVideoIcon = m.IsAnyoneVideoStreaming && !m.IsAnyoneTalking;
    var iconMode = showVideoIcon ? "video" : "audio";
    var tooltip = isListening ? "Listening..." : "Listen";
    var cls = isListening ? " listening" : "";
    var joinButtonText = "Join muted";
    var showVideoPanelToggle = m.IsAnyoneVideoStreaming && isParticipating;
}

<div @ref="Ref"
     class="chat-activity-panel @cls"
     @onclick:preventDefault="true"
     @onclick:stopPropagation="true"
     @onclick="@Click">
    @if (!isParticipating) {
        @* Camera/audio icon - shown when not participating *@
        <div class="c-icon hideable">
            <chat-activity-panel-icon-svg size="@iconSize" isActive="@isActive" mode="@iconMode"/>
        </div>
    }
    @* Talking heads - shown when anyone is talking *@
    @if (m.IsAnyoneTalking) {
        <ChatActivities
            @key="@chat.Id.Value"
            Chat="@chat"
            MaxCount="@MaxCount"
            IsListening="@isListening"/>
    }
    <div class="flex-1"></div>
    <div class="c-buttons">
        @if (showVideoPanelToggle) {
            <HeaderButton
                Class="video-panel-toggle"
                StopPropagation="true"
                PreventDefault="true"
                Click="@OnToggleVideoPanel">
                @if (m.IsVideoPanelCollapsed) {
                    <i class="icon-video-expand"></i>
                } else {
                    <i class="icon-video-collapse"></i>
                }
            </HeaderButton>
        }
        @if (!isListening && !isRecordingHere && !isVideoRecordingHere) {
            <Button
                Class="btn-sm btn-transparent unhovered not-listening"
                StopPropagation="true"
                PreventDefault="true"
                Click="@(() => OnJoinClick(chat.Id))"
                Tooltip="@tooltip"
                TooltipPosition="FloatingPosition.Bottom">
                @joinButtonText
            </Button>
        } else if (isRecordingHere || isVideoRecordingHere) {
            <HeaderButton
                Class="talking"
                StopPropagation="true"
                PreventDefault="true"
                Click="@(() => OnHungUpClick(chat.Id))"
                Tooltip="@tooltip"
                TooltipPosition="FloatingPosition.Bottom">
                <i class="icon-phone-off"></i>
            </HeaderButton>
        } else {
            const double countdownSeconds = 30;
            const double circumference = 100.531;
            var remainingSeconds = m.StopListeningAt.HasValue
                ? Math.Max(0, (m.StopListeningAt.Value - Hub.Clocks.CpuClock.Now).TotalSeconds)
                : 0;
            var isCountingDown = remainingSeconds > 0 && remainingSeconds <= countdownSeconds;
            var dashOffset = isCountingDown
                ? (circumference * (1 - remainingSeconds / countdownSeconds)).ToString("F2", CultureInfo.InvariantCulture)
                : "0";
            <div class="listening-timer">
                @if (isCountingDown) {
                    <svg class="timer-ring" viewBox="0 0 36 36"
                         data-bubble="@(BubbleRef.New<ListeningTimerBubble>())"
                         data-bubble-priority="115"
                         data-bubble-placement="@(FloatingPosition.Bottom.ToPositionString())">
                        <circle class="timer-ring-bg"
                                cx="18" cy="18" r="16"/>
                        <circle class="timer-ring-circle"
                                cx="18" cy="18" r="16"
                                style="stroke-dashoffset: @(dashOffset)"/>
                    </svg>
                }
                <HeaderButton
                    Class="listening"
                    StopPropagation="true"
                    PreventDefault="true"
                    Click="@(() => OnStopListeningClick(chat.Id))"
                    Tooltip="@tooltip"
                    TooltipPosition="FloatingPosition.Bottom">
                    @if (isCountingDown) {
                        var remaining = (int)Math.Ceiling(remainingSeconds);
                        var mm = remaining / 60;
                        var ss = remaining % 60;
                        <span class="timer-icon-swap">
                            <i class="icon-listen swap-a"></i>
                            <span class="swap-b timer-countdown-text">@($"{mm:D2}:{ss:D2}")</span>
                        </span>
                    } else {
                        <i class="icon-listen"></i>
                    }
                </HeaderButton>
            </div>
        }
    </div>
</div>

@code {
    private static readonly string JSCreateMethod = $"{BlazorUIAppModule.ImportName}.ChatActivityPanel.create";
    private readonly TaskSerializer _taskSerializer = new();
    private CpuTimestamp _lastActionAt;

    private ElementReference Ref { get; set; }
    private IJSObjectReference JSRef { get; set; } = null!;

    private LiveStreamUI LiveStreamUI => Hub.LiveStreamUI;
    private ChatAudioUI ChatAudioUI => Hub.ChatAudioUI;
    private ChatVideoUI ChatVideoUI => Hub.ChatVideoUI;
    private ActiveChatsUI ActiveChatsUI => Hub.ActiveChatsUI;

    private bool IsVideoPanelCollapsed => State.Value?.IsVideoPanelCollapsed == true;

    [CascadingParameter] public ScreenSize ScreenSize { get; set; }
    [Parameter, EditorRequired] public Chat Chat { get; set; }
    [Parameter] public int MaxCount { get; set; } = 5;
    [Parameter] public EventCallback Click { get; set; }

    public override async ValueTask DisposeAsync() {
        await JSRef.DisposeSilentlyAsync("dispose");
        JSRef = null!;
        await base.DisposeAsync();
    }

    protected override async Task OnAfterRenderAsync(bool firstRender) {
        if (firstRender && ScreenSize.IsNarrow())
            JSRef = await JS.InvokeAsync<IJSObjectReference>(JSCreateMethod, Ref);
    }

    protected override async Task<Model?> ComputeState(CancellationToken cancellationToken) {
        var chat = Chat;
        var chatId = chat.Id;
        var audioState = await ChatAudioUI.GetState(chatId).ConfigureAwait(false);
        var isAnyoneTalking = await LiveStreamUI.IsAnyoneStreaming(chatId, cancellationToken).ConfigureAwait(false);
        var isAnyoneVideoStreaming = await ChatVideoUI.IsAnyoneVideoStreaming(chatId, cancellationToken).ConfigureAwait(false);
        var stopListeningAt = audioState.IsListening
            ? await ChatAudioUI.GetStopListeningAt(chatId, cancellationToken).ConfigureAwait(false)
            : null;
        if (stopListeningAt.HasValue) {
            var remaining = (stopListeningAt.Value - Hub.Clocks.CpuClock.Now).TotalSeconds;
            if (remaining > 30)
                Computed.GetCurrent().Invalidate(TimeSpan.FromSeconds(remaining - 30));
            else if (remaining > 0) {
                // Align invalidation to the next second boundary for consistent countdown ticks
                var fractional = remaining - Math.Floor(remaining);
                var delay = fractional > 0.05 ? fractional : fractional + 1.0;
                Computed.GetCurrent().Invalidate(TimeSpan.FromSeconds(delay));
            }
        }

        var isOwnVideoRecording = await ChatVideoUI.GetRecordingChatId(cancellationToken).ConfigureAwait(false) == chatId;
        var isVideoPanelCollapsed = await ChatVideoUI.GetIsVideoPanelCollapsed(cancellationToken).ConfigureAwait(false);

        return new (chat, audioState, isAnyoneTalking, isAnyoneVideoStreaming, isOwnVideoRecording, stopListeningAt, isVideoPanelCollapsed);
    }

    private async Task OnJoinClick(ChatId chatId) {
        if (IsUpdating(chatId))
            return;

        // User is not participating — join with listening only (no recording, no camera)
        _ = ChatAudioUI.SetListeningState(chatId, true);
        // Open video panel to see others' streams
        var isAnyoneVideoStreaming = await ChatVideoUI.IsAnyoneVideoStreaming(chatId, CancellationToken.None).ConfigureAwait(false);
        if (isAnyoneVideoStreaming)
            ChatVideoUI.SetWatching(chatId);
    }

    private async Task OnHungUpClick(ChatId chatId) {
        if (IsUpdating(chatId))
            return;

        // User is already participating — stop everything
        var audioState = await ChatAudioUI.GetState(chatId).ConfigureAwait(false);
        if (audioState.IsRecording)
            StopRecording(chatId);
        // Stop video recording and watching
        ChatVideoUI.SetRecordingChatId(null);
        ChatVideoUI.SetWatching(null);
        // Stop listening
        _ = ChatAudioUI.SetListeningState(chatId, false);
    }

    private void OnStopListeningClick(ChatId chatId) {
        if (IsUpdating(chatId))
            return;

        // Stop listening
        _ = ChatAudioUI.SetListeningState(chatId, false);
    }

    private bool IsUpdating(ChatId chatId)
        => chatId != Chat.Id;

    private void OnToggleVideoPanel()
        => ChatVideoUI.SetVideoPanelCollapsed(!IsVideoPanelCollapsed);

    private void StopRecording(ChatId chatId) {
        if (!IsValidAction())
            return;

        _ = _taskSerializer.Enqueue(async _ => {
            var activeChats = ActiveChatsUI.ActiveChats.Value;
            var recordingChatId = activeChats.FirstOrDefault(c => c.IsRecording)?.ChatId;
            if (recordingChatId == chatId)
                await ChatAudioUI.SetRecordingChatId(null).ConfigureAwait(false); // .ConfigureAwait(false) is fine here
        }, mustAbortPreviousTasks: true);
    }

    private bool IsValidAction() {
        if (_lastActionAt.Elapsed < TimeSpan.FromMilliseconds(500))
            return false; // Rapid action sequence is likely a mistake

        _lastActionAt = CpuTimestamp.Now;
        return true;
    }

    // Nested types

    public sealed record Model(
        Chat Chat,
        ChatAudioState AudioState,
        bool IsAnyoneTalking,
        bool IsAnyoneVideoStreaming,
        bool IsOwnVideoRecording = false,
        Moment? StopListeningAt = null,
        bool IsVideoPanelCollapsed = false) {
        public bool IsListening => AudioState.IsListening;
        public bool IsRecordingHere => AudioState.IsRecording;
    }
}
