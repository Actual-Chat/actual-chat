@inherits MarkupViewBase<ParagraphMarkup>

@if (Markup.IsEmpty) {
    <p class="paragraph-markup blank-line-markup"></p>
} else {
    <p class="paragraph-markup">
        <MarkupView Markup="@Markup.Content"/>
    </p>
}
