@using ActualChat.Hosting

<ModalHost />
<MenuHost />
<BubbleHost />
@if (!BrowserInfo.IsTouchCapable) {
    <TooltipHost />
}

<RenderIntoSlot Name="@LayoutSlots.MainLeftDrawer">
    <LeftPanel/>
</RenderIntoSlot>

<RenderIntoStack Name="@LayoutSlots.SubHeader" Order="-1000_000" Key="HistoricalPlaybackSubHeader">
    <HistoricalPlaybackSubHeader/>
    <RecordingSubHeader/>
    <TranslationSubHeader/>
</RenderIntoStack>

@if (!HostInfo.HostKind.IsMauiApp()) {
    <PageHeader />
}

@code {

    [Inject] private HostInfo HostInfo { get; init; } = null!;
    [Inject] private BrowserInfo BrowserInfo { get; init; } = null!;
    [Inject] private LoadingUI LoadingUI { get; init; } = null!;

    protected override void OnAfterRender(bool firstRender) {
        if (firstRender)
            LoadingUI.MarkRendered();
    }
}
