@inherits ComponentBase<AppUIHub>

<div class="chat-footer readonly">
    <div class="c-content">
        @(Text ?? L.ChatFooter_ReadOnly)
    </div>
</div>

@code {
    [Parameter] public string? Text { get; set; }
}
