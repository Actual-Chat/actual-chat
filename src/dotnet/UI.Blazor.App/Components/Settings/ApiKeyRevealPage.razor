@inherits ComponentBase<AppUIHub>
@inject IStringLocalizer<Strings> L

<FormBlock>
    <p class="c-title">@L["ApiKeyReveal_CopyWarning"]</p>
    <div class="api-key-container">
        <code>@_model.ApiKey</code>
        <CopyToClipboard
            IsSquare="true"
            ButtonClass="btn-sm btn-transparent"
            Text="@_model.ApiKey"/>
    </div>
</FormBlock>

@code {
    private Model _model = null!;

    [CascadingParameter] public DiveInModalPageContext Context { get; set; } = null!;
    [CascadingParameter] public Modal Modal { get; set; } = null!;

    protected override void OnInitialized() {
        _model = Context.GetModel<Model>();
        Context.Title = L["ApiKeyReveal_Title"].Value;
        Context.Buttons = [DialogButtonInfo.CreateSubmitButton(L["Common_Close"].Value, OnClose)];
    }

    private void OnClose()
        => Modal.Close();

    public sealed record Model(string ApiKey);
}
