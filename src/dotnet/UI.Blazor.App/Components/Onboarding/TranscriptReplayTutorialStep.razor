@namespace ActualChat.UI.Blazor.App.Components
@inherits SimpleStep<AppUIHub>;
@if (CurrentStep != this) {
    return;
}

<div class="onboarding-tutorial tutorial-2">
    <img draggable="false" src="/dist/images/onboarding/tutorial-2.png" alt="Tutorial slide #2">
    <div class="c-title">
        <p>
            @L.Onboarding_Tutorial2Title
        </p>
    </div>
    <p class="c-description">
        @L.Onboarding_Tutorial2Description
    </p>
</div>

@code {
    private OnboardingUI OnboardingUI => Hub.OnboardingUI;

    public override bool CanSkip => true;

    public override bool IsCompleted => OnboardingUI.UserSettings.Value.IsTranscriptReplayTutorialStepCompleted;

    protected override void MarkCompleted()
        => OnboardingUI.UpdateUserSettings(OnboardingUI.UserSettings.Value with { IsTranscriptReplayTutorialStepCompleted = true });

    protected override Task<bool> Validate()
        => Task.FromResult(true);

    protected override ValueTask OnSkip()
        => ValueTask.CompletedTask;

    protected override Task<bool> Save()
        => Task.FromResult(true);
}
