@namespace ActualChat.UI.Blazor.App.Components
@inherits SimpleStep<AppUIHub>;
@if (CurrentStep != this) {
    return;
}

<div class="onboarding-tutorial tutorial-3">
    <img draggable="false" src="/dist/images/onboarding/tutorial-3.png" alt="Tutorial slide #3">
    <div class="c-title">
        <p>
            @L.Onboarding_Tutorial3Title1
        </p>
        <p>
            @L.Onboarding_Tutorial3Title2
        </p>
    </div>
    <p class="c-description">
        @L.Onboarding_Tutorial3Description
    </p>
</div>

@code {
    private OnboardingUI OnboardingUI => Hub.OnboardingUI;

    public override bool CanSkip => true;

    public override bool IsCompleted => OnboardingUI.UserSettings.Value.IsPlacesTutorialStepCompleted;

    protected override void MarkCompleted()
        => OnboardingUI.UpdateUserSettings(OnboardingUI.UserSettings.Value with { IsPlacesTutorialStepCompleted = true });

    protected override Task<bool> Validate()
        => Task.FromResult(true);

    protected override ValueTask OnSkip()
        => ValueTask.CompletedTask;

    protected override Task<bool> Save()
        => Task.FromResult(true);
}
