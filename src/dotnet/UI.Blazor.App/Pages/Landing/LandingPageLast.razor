<div class="bg-wrapper">
    @if (!ScreenSize.IsNarrow()) {
        <img draggable="false" class="wide-image" src="/dist/images/voxt-galaxy-horizontal.svg" alt="voxt">
    } else {
        <img draggable="false" class="narrow-image" src="/dist/images/voxt-galaxy-vertical.svg" alt="voxt">
    }
</div>

<div class="c-content">
    <div class="c-top">
        <div class="main-image">
            <img draggable="false" src="/dist/images/landing/page-last-image.svg" alt="">
        </div>
        <div class="main-text">
            <span class="title-text">
                Talk the way humans
            </span>
            <span class="page-last-handwrite">
                were meant to.
            </span>
            @* <button class="btn-download light" @onclick="@PremiumFeaturesClick"> *@
            @*     <span>Learn more</span> *@
            @* </button> *@
        </div>
    </div>

    <div class="c-bottom">
        <div class="c-btn-group">
            @if (IsGuest) {
                <button class="btn-download web-signin" @onclick="@SignInClick">
                    <span>Sign in to web app</span>
                    <i class="icon-monitor text-2xl font-thin"></i>
                </button>
            }
            <DownloadButton
                Title="Get mobile/desktop app"
                Click="@DownloadClick"
                Class="download-app"/>
        </div>
        <LandingLinks/>
    </div>
    <LandingFooter/>

</div>

@code {
    [CascadingParameter] public ScreenSize ScreenSize { get; set; }

    [Parameter] public bool IsGuest { get; set; }
    [Parameter] public EventCallback SignInClick { get; set; }
    [Parameter] public EventCallback PremiumFeaturesClick { get; set; }
    [Parameter] public EventCallback DownloadClick { get; set; }
}
