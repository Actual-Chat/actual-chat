@inherits UserBadgeBase
@{
    var model = State.LatestNonErrorValue;
}

@if (model.UserInfo == null) {
    <Badge Color="Color.Light">
        Loading...
    </Badge>
} else {
    <Badge Color="model.IsOnline ?? true ? Color.Primary : Color.Secondary">
        @model.UserInfo.Name
    </Badge>
}
